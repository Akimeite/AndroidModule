language: android
dist: trusty
jdk: oraclejdk8
android:
  components:
    - build-tools-29.0.2
    - android-29

  licenses:
    - 'android-sdk-preview-license-52d11cd2'
    - 'android-sdk-license-.+'
    - 'google-gdk-license-.+'

before_script:
  - touch local.properties

script:
  - ./gradlew clean assembleDebug